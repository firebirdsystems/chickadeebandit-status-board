{
  "name": "chickadeebandit-status-board",
  "version": "1.0.0",
  "description": "Who's Home — a shared family presence/status board",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node dev.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
